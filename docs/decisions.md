Groq over Ollama and why:
openai/gpt-oss-20b chosen from what the account actually offers, the provider interface existing so Ollama is additive, and a note that Groq retires models without warning so GROQ\_MODEL lives in .env.

